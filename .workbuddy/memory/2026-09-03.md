# 2026-09-03

## fsviewer：按工作区隔离右侧边栏（接手 zcode 未完成进度）

目标：右侧边栏状态/文件树/文件开关/浏览器/侧边聊天全部按工作区独立，切换工作区整体跟随。

### 完成的改动（src/client.js）
- 面板会话按原生 `workspaceId` 存于 `panels`（localStorage key `fsviewer/tabs.v4`）；每条含 `tabs / browser / chats / expanded / treeOn / treeW / panelOpen / path`。
- 工作区解析 `resolveCurrentWs()` 对齐原生：当前会话 `sessionIds` 判定所属 workspace > `workspaces.list.recentWorkspaceId` > cwd 匹配。已核对 dsh 类型 `WorkspaceListState{items,recentWorkspaceId}` 与 `WorkspaceView{sessionIds,workspaceId,path}` 字段确实存在。
- `FileTreePanel` 订阅 `sessions.list` + `workspaces.list`，任一变化经单一 `apply()` 调 `setWorkspace(key,path)`；`<FilesView key={wsKey}>` 切工作区即换实例，树展开/分支/筛选/文件缓存/树栏宽随工作区隔离。
- 修正上一会话中断遗留：`pendingDirJump` 声明缺失（FilesView 挂载消费）；`openDirInPanel` 右栏收起时暂存跳转目标、挂载后消费，兜底交还系统打开。
- 删除死代码 `panelFileDispatch`（仅赋值无消费）。
- 构建 `npm run build` + 冒烟 `npm test` 均通过。README 新增按工作区隔离特性说明。
